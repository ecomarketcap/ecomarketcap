export type Coin = {
       paprika_id: string;
       gecko_id: string;
       name: string;
       symbol: string;
       rank: number;
       is_new: boolean;
       is_active: boolean;
       svg: string;
       chartSvgIndex: number | null;
};